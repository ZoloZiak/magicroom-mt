---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;

const baseClasses = 'rounded-xl border bg-card text-card-foreground shadow';
---

<div class:list={[baseClasses, className]} {...rest}>
  <slot />
</div>