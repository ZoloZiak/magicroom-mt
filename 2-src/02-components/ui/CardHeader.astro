---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;
---

<div class:list={["flex flex-col space-y-1.5 p-6", className]} {...rest}>
  <slot />
</div>