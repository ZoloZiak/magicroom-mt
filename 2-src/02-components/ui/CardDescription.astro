---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;
---

<p class:list={["text-sm text-muted-foreground", className]} {...rest}>
  <slot />
</p>