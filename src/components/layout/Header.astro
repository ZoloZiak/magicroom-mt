---
import { Button } from '@/components/ui/button';
import { BOOKING_PAGE_HREF, NAV_LINKS, PHONE_DISPLAY, PHONE_HREF, RESPONSE_PROMISE } from '@/data/site';

const currentPath = Astro.url.pathname;
const bookingHref = currentPath === '/' ? '#booking' : BOOKING_PAGE_HREF;
---

<header class="sticky top-0 z-50 w-full border-b border-border/70 bg-background/80 shadow-[0_10px_40px_rgba(61,42,46,0.05)] backdrop-blur-xl">
  <div class="hidden border-b border-border/50 bg-white/60 md:block">
    <div class="container mx-auto flex h-10 items-center justify-between px-4 text-[11px] font-medium uppercase tracking-[0.24em] text-muted-foreground">
      <p>Skúška svadobných šiat 12 € / 60 min</p>
      <div class="flex items-center gap-6">
        <p>Martin • na objednávku</p>
        <a href={PHONE_HREF} class="hover:text-foreground">{PHONE_DISPLAY}</a>
        <p>{RESPONSE_PROMISE}</p>
      </div>
    </div>
  </div>

  <nav class="container mx-auto flex h-[4.5rem] items-center justify-between px-4">
    <a href="/" class="group flex items-center gap-3" aria-label="MagicRoom domov">
      <img
        src="/images/logo.jpeg"
        alt="MagicRoom logo"
        class="h-11 w-11 rounded-full object-cover shadow-[0_12px_30px_rgba(122,68,88,0.15)]"
        width="44"
        height="44"
      />
      <div class="hidden sm:block">
        <span class="block font-display text-[1.45rem] leading-none transition-colors group-hover:text-primary">MagicRoom</span>
        <span class="mt-1 block text-[10px] uppercase tracking-[0.34em] text-muted-foreground">bridal concierge • Martin</span>
      </div>
    </a>

    <ul class="hidden items-center gap-8 md:flex">
      {NAV_LINKS.map((link) => (
        <li>
          <a
            href={link.href}
            aria-current={currentPath === link.href ? 'page' : undefined}
            class:list={[
              'relative text-sm font-medium tracking-[0.08em] after:absolute after:-bottom-2 after:left-0 after:h-px after:w-full after:origin-left after:bg-cta after:transition-transform after:duration-300',
              currentPath === link.href
                ? 'text-foreground after:scale-x-100'
                : 'text-foreground/70 after:scale-x-0 hover:text-foreground hover:after:scale-x-100',
            ]}
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>

    <div class="hidden md:block">
      <Button asChild variant="cta" size="sm">
        <a href={bookingHref}>Zarezervuj skúšku</a>
      </Button>
    </div>

    <div class="md:hidden">
      <button
        id="mobile-menu-toggle"
        style="cursor:pointer;border-radius:8px;padding:8px;background:none;border:none;"
        aria-label="Otvoriť navigáciu"
      >
        <svg xmlns="http://www.w3.org/2000/svg" style="width:24px;height:24px;" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" /></svg>
      </button>
    </div>
  </nav>
</header>

<div id="mobile-menu-root"></div>

<script>
  const NAV_LINKS = [
    { href: '/', label: 'Domov' },
    { href: '/o-nas', label: 'O nás' },
    { href: '/sluzby', label: 'Služby' },
    { href: '/prenajom-dekoracii', label: 'Dekorácie' },
    { href: '/komisny-predaj', label: 'Komis' },
    { href: '/blog', label: 'Blog' },
    { href: '/kontakt', label: 'Kontakt' },
  ];

  const PHONE_HREF = 'tel:+421950490323';
  const PHONE_DISPLAY = '+421 950 490 323';
  const RESPONSE_PROMISE = 'Odpisujeme do 24 hodín.';
  const currentPath = window.location.pathname;
  const bookingHref = currentPath === '/' ? '#booking' : '/kontakt#booking';

  const root = document.getElementById('mobile-menu-root');
  const toggleBtn = document.getElementById('mobile-menu-toggle');
  let menuEl = null;

  function createMenu() {
    const el = document.createElement('div');
    el.id = 'mobile-drawer';
    el.setAttribute('role', 'dialog');
    el.setAttribute('aria-modal', 'true');
    el.innerHTML = `
      <div id="mobile-backdrop" style="position:fixed;inset:0;z-index:9998;background:rgba(0,0,0,0.5);display:none;"></div>
      <div id="mobile-panel" style="position:fixed;top:0;right:0;z-index:9999;height:100vh;width:300px;background:#FDFBF9;box-shadow:-4px 0 30px rgba(0,0,0,0.15);transform:translateX(100%);transition:transform 0.3s ease;overflow-y:auto;">
        <div style="padding:20px;border-bottom:1px solid #E8D0D8;display:flex;align-items:center;justify-content:space-between;">
          <div style="display:flex;align-items:center;gap:12px;">
            <img src="/images/logo.jpeg" alt="MagicRoom" style="width:40px;height:40px;border-radius:50%;object-fit:cover;" />
            <div>
              <span style="display:block;font-family:Satoshi,system-ui;font-size:1.5rem;font-weight:600;">MagicRoom</span>
              <span style="display:block;font-size:10px;text-transform:uppercase;letter-spacing:0.28em;color:#6B5E57;">bridal concierge &bull; Martin</span>
            </div>
          </div>
          <button id="mobile-close-btn" style="cursor:pointer;padding:8px;background:none;border:none;border-radius:8px;" aria-label="Zavrieť">
            <svg xmlns="http://www.w3.org/2000/svg" style="width:24px;height:24px;" fill="none" viewBox="0 0 24 24" stroke="#2D2421" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" /></svg>
          </button>
        </div>
        <div style="padding:12px 20px;">
          <div style="border-radius:16px;border:1px solid #E8D0D8;background:rgba(255,255,255,0.8);padding:12px 16px;font-size:14px;color:#6B5E57;">
            <p style="font-weight:500;color:#2D2421;margin:0;">${RESPONSE_PROMISE}</p>
            <a href="${PHONE_HREF}" style="display:block;margin-top:4px;color:#B87A8E;text-decoration:none;">${PHONE_DISPLAY}</a>
          </div>
        </div>
        <div style="padding:12px 20px 0;">
          <p style="font-size:11px;text-transform:uppercase;letter-spacing:0.28em;color:rgba(184,122,142,0.7);margin:0;">Navigácia</p>
        </div>
        <ul style="list-style:none;margin:0;padding:0 20px;">
          ${NAV_LINKS.map(link => `
            <li style="border-bottom:1px solid rgba(232,208,216,0.5);padding:12px 0;">
              <a href="${link.href}" style="display:block;padding:8px 0;font-size:18px;font-weight:500;color:${currentPath === link.href ? '#B85C4A' : '#2D2421'};text-decoration:none;"
                 onmouseover="this.style.color='#B85C4A'" onmouseout="this.style.color='${currentPath === link.href ? '#B85C4A' : '#2D2421'}'">
                ${link.label}
              </a>
            </li>
          `).join('')}
        </ul>
        <div style="padding:20px;border-top:1px solid #E8D0D8;">
          <a href="${bookingHref}" style="display:block;text-align:center;padding:12px 24px;background:#B85C4A;color:#fff;border-radius:12px;font-weight:500;text-decoration:none;font-size:15px;">
            Zarezervuj skúšku
          </a>
          <a href="${PHONE_HREF}" style="display:block;margin-top:16px;text-align:center;font-size:14px;font-weight:500;color:#6B5E57;text-decoration:none;">
            Alebo zavolajte priamo
          </a>
        </div>
      </div>
    `;
    document.body.appendChild(el);
    menuEl = el;

    document.getElementById('mobile-backdrop')?.addEventListener('click', closeMenu);
    document.getElementById('mobile-close-btn')?.addEventListener('click', closeMenu);
    el.querySelectorAll('a[href]').forEach(a => {
      a.addEventListener('click', closeMenu);
    });
  }

  function openMenu() {
    if (!menuEl) createMenu();
    const backdrop = document.getElementById('mobile-backdrop');
    const panel = document.getElementById('mobile-panel');
    if (backdrop) backdrop.style.display = 'block';
    document.body.style.overflow = 'hidden';
    requestAnimationFrame(() => {
      if (panel) panel.style.transform = 'translateX(0)';
    });
  }

  function closeMenu() {
    const backdrop = document.getElementById('mobile-backdrop');
    const panel = document.getElementById('mobile-panel');
    if (panel) panel.style.transform = 'translateX(100%)';
    document.body.style.overflow = '';
    setTimeout(() => {
      if (backdrop) backdrop.style.display = 'none';
    }, 300);
  }

  toggleBtn?.addEventListener('click', openMenu);
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeMenu();
  });
</script>
