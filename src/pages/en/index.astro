---
export const prerender = true;
import HomeTemplate from '@/components/templates/HomeTemplate.astro';
import type { Language } from '@/lib/i18n';

const language: Language = 'en';
---

<HomeTemplate language={language} />
